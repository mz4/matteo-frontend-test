const Subex = ({ clearCounter }) => {
  return (
    <>
      Hello! From here you can clear the counter
      <div>
        <button onClick={clearCounter}>Clear Counter!</button>
      </div>
    </>
  )
}

export default Subex;